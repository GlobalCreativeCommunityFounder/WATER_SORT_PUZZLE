# -*- mode: python ; coding: utf-8 -*-


block_cipher = None


a = Analysis(['water_sort_puzzle.py'],
             pathex=["D:\\For Vio's New Facebook Groups\\WATER_SORT_PUZZLE\\WATER_SORT_PUZZLE"],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          [],
          exclude_binaries=True,
          name='water_sort_puzzle',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               upx_exclude=[],
               name='water_sort_puzzle')
